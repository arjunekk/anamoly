[pytest]
pythonpath = .
testpaths = tests
python_files = test_*.py
markers =
    slow: tests that take significant time (model loading, full pipeline runs)
    integration: tests that require external resources (database, filesystem)